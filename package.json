{
  "name": "yks-defterim",
  "private": true,
  "main": "desktop/main.cjs",
  "version": "4.4.0",
  "packageManager": "npm@10.9.8",
  "engines": {
    "node": ">=22 <25",
    "npm": ">=10 <12"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "typecheck": "tsc --noEmit",
    "infra:check": "node scripts/verify-infrastructure.mjs",
    "build:assets": "node scripts/prepare-anatomy-assets.mjs && vite build --mode web && node scripts/copy-legacy-assets.mjs && node scripts/verify-dist.mjs web",
    "build:android": "node scripts/prepare-anatomy-assets.mjs && vite build --mode android && node scripts/copy-legacy-assets.mjs && node scripts/verify-dist.mjs android",
    "build:desktop": "npm run typecheck && node scripts/prepare-anatomy-assets.mjs && vite build --mode desktop && node scripts/copy-legacy-assets.mjs && node scripts/verify-dist.mjs desktop",
    "desktop:pack": "npm run build:desktop && electron-builder --config desktop/electron-builder.yml --win nsis --x64 --publish never && node scripts/verify-desktop.mjs",
    "desktop:dev": "npm run build:desktop && electron .",
    "desktop:verify": "node --test tests/desktop*.test.js",
    "build": "npm run typecheck && node scripts/prepare-anatomy-assets.mjs && vite build --mode web && node scripts/copy-legacy-assets.mjs && node scripts/verify-dist.mjs web",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/*.test.js",
    "check": "npm run infra:check && npm run typecheck && npm test && npm run build:assets",
    "release:check": "npm run check && node scripts/verify-release.mjs && node scripts/verify-v440-production.mjs",
    "android:sync": "npm run build:android && cap sync android",
    "android:prepare": "npm run android:sync && node scripts/prepare-android-release.mjs",
    "android:verify": "node --test tests/android-release.test.js"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0",
    "@types/three": "0.185.0",
    "electron": "44.2.0",
    "electron-builder": "26.16.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "@capacitor/android": "8.5.0",
    "@capacitor/core": "8.5.0",
    "dexie": "^4.4.5",
    "firebase": "12.17.1",
    "three": "0.185.1"
  }
}
