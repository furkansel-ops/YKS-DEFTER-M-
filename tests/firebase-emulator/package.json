{
  "name": "yks-cloud-emulator-safety-tests",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22 <25" },
  "scripts": {
    "test": "firebase emulators:exec --project demo-yks-sync --config ../../firebase.emulators.json --only auth,firestore \"node --test --test-concurrency=1 cloud.integration.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.17.1",
    "firebase-tools": "15.29.0"
  }
}
